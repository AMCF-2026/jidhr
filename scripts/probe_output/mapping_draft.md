# CSuite -> HubSpot mapping draft

Generated by `scripts/probe_apis.py`. Overwritten on every run.

Generated: 2026-09-08T12:41:03

Section 1 is what the repo does **today**, read out of the sync code. Section 2 is everything the probe saw that nothing connects yet. Nothing here has been decided — it is a worksheet.

## 1. In code today

| CSuite endpoint | CSuite field | -> HubSpot property | Status | Source | Note |
| --- | --- | --- | --- | --- | --- |
| `donation/list` | `donation_amount (sum by profile_id)` | `lifetime_giving` | in code | `sync/donations.py` | float -> str, 2dp |
| `donation/list` | `donation_id (count by profile_id)` | `donation_count` | in code | `sync/donations.py` | int -> str |
| `donation/list` | `donation_amount (most recent by donation_date)` | `last_donation_amount` | in code | `sync/donations.py` | float -> str, 2dp |
| `donation/list` | `donation_date (max)` | `last_donation_date` | in code | `sync/donations.py` | YYYY-MM-DD -> YYYY-MM-DDT00:00:00.000Z |
| `donation/list` | `profile_id` | `(join key only)` | in code | `sync/donations.py` | links donations to profiles |
| `profile/list` | `profile_id` | `csuite_profile_id` | in code | `sync/donations.py` | int -> str |
| `profile/list` | `primary_email` | `email (match key)` | in code | `sync/donations.py` | lowercased + stripped; contact lookup key for the whole sync |
| `event/list/dates` | `event_description (falls back to event_name)` | `marketing event .eventName` | in code | `sync/events.py` | CSuite event_name is generic e.g. 'Event - Other' |
| `event/list/dates` | `event_description + location` | `marketing event .eventDescription` | in code | `sync/events.py` | joined with ' | ' |
| `event/list/dates` | `event_date + start_time` | `marketing event .startDateTime` | in code | `sync/events.py` | messy time strings parsed; falls back to midnight |
| `event/list/dates` | `event_date + start_time + 2h` | `marketing event .endDateTime` | in code | `sync/events.py` | default 2h duration |
| `event/list/dates` | `event_date_id (falls back to event_id)` | `marketing event .externalEventId` | in code | `sync/events.py` | prefixed 'csuite-'; dedupe key |
| `event/list/dates` | `event_type_code` | `marketing event .eventType` | in code | `sync/events.py` | mapped through map_event_type, default 'Other' |
| `event/list/dates` | `location` | `(folded into eventDescription)` | in code | `sync/events.py` | no dedicated HubSpot property |
| `event/list/dates` | `archived` | `(filter only — never written)` | in code | `sync/events.py` | archived events are skipped |
| `(config)` | `DEFAULT_EVENT_OWNER_ID` | `marketing event .eventOrganizer` | in code | `sync/events.py` | constant, not a CSuite field |
| `profile/list` | `newsletter (== 1)` | `communication-preferences subscription 1265988358` | in code | `sync/newsletter.py` | opt-in only; never unsubscribes |
| `profile/list` | `primary_email` | `subscribe_contact(email)` | in code | `sync/newsletter.py` | lowercased + stripped |
| `profile/list` | `name` | `(logged only — never written)` | in code | `sync/newsletter.py` |  |
| `profile/create/individual (response)` | `profile_id` | `csuite_profile_id` | in code | `intents/daf_workflow.py` | int -> str; write-direction workflow |
| `funit/create (response)` | `funit_id` | `csuite_fund_id` | in code | `intents/daf_workflow.py` | int -> str; write-direction workflow |
| `(HubSpot form submission)` | `firstname / lastname / email / phone` | `-> CSuite profile/create/individual` | in code | `intents/daf_workflow.py` | reverse direction: HubSpot -> CSuite |

## 2. Candidates

### 2a. CSuite fields with no HubSpot counterpart

_No CSuite records were returned, so no candidates could be derived. Re-run with working credentials._

### 2b. HubSpot custom properties nothing writes to

_No custom contact properties returned. Re-run with working credentials._

### 2c. Custom company properties (nothing in the repo writes to companies at all)

_None returned._
